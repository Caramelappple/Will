# 작업 규칙

AI에게 작업을 시킬 때 지켜야 할 것들. 이시온(LSO) 브랜치 기준이다.

---

## 보고

**스크립트를 만들거나 고치면 어떤 파일인지 반드시 밝힌다.**

여러 파일을 건드렸으면 하나도 빠뜨리지 않는다. 팀 저장소라 내가 만진 파일을
다른 사람이 같이 보고 있을 수 있고, 병합 전에 무엇이 바뀌었는지 알아야 한다.

형식은 이렇게 한다.

```
만든 것
  Assets/_Scripts/LSO/Reward/LSO_WillNote.cs

고친 것
  Assets/_Scripts/LSO/Reward/LSO_RewardBox.cs    유언 종이 단계 추가
  Assets/_Scripts/LSO/Reward/LSO_RewardCard.cs   호버 대상 분리

지운 것
  (없음)
```

- **경로를 다 적는다.** 파일 이름만 적으면 어느 폴더인지 찾아야 한다.
- **한 줄 요약을 붙인다.** 무엇을 왜 고쳤는지.
- 내 담당(LSO)이 아닌 파일을 건드렸으면 **그 사실을 따로 짚는다.**

---

## 코드

- **SOLID 원칙**을 따른다.
- **God Class 금지.** 한 클래스가 여러 일을 하기 시작하면 쪼갠다.
- **만드는 `.cs` 파일 앞에 `LSO_` 를 붙인다.** (`LSO_Test.cs`)
- 자리·상태를 정하는 **주체는 하나여야 한다.** 두 곳이 같은 값을 쓰면
  어긋났을 때 어느 쪽이 맞는지 정할 방법이 없다.
- **조용한 실패를 만들지 않는다.** 배선이 빠졌으면 경고를 남긴다.

---

## 건드리지 말 것

- **`.env` 수정 금지**
- **기존 파일의 네임스페이스를 바꾸지 않는다.** 폴더와 안 맞아 보여도 그대로 둔다.
- **파일을 옮기지 않는다.** 옮기면 네임스페이스를 건드리게 된다.
- 전 프로젝트를 훑는 **일괄 `using` 정리를 하지 않는다.** 내가 만지는 파일에서
  컴파일에 필요한 만큼만 한다.

새로 만드는 파일은 그 폴더에 이미 있는 네임스페이스를 그대로 따른다.
정말 바꿔야 할 것 같으면 **먼저 말하고 승인을 받는다.**

### 왜

`KTH_DeckManager.cs`를 네임스페이스로 감싸고 전체를 들여쓴 적이 있다.
의미상 차이는 몇 줄인데 diff는 810줄이 됐고, 머지에서 두 버전이 섞여
파일이 깨진 채로 커밋됐다. 자세한 건 `GIT_협업규칙.md` 참고.

---

## 담당

| 이니셜 | 사람 |
|---|---|
| LSO | 이시온 (나) |
| LDY | 이도윤 |
| KTH | 김태호 |
| DLJ | 주다림 |
| JSJ | 정성진 |

**남의 폴더 파일은 되도록 건드리지 않는다.** 꼭 필요하면 고치되,
보고할 때 반드시 짚고 담당자에게 알릴 수 있게 한다.

---

## 커밋

- **포맷만 바꾸는 커밋과 로직을 바꾸는 커밋을 섞지 않는다.**
- 머지 충돌을 해결한 뒤에는 **유니티 콘솔에서 에러 0을 확인하고** push한다.
- 씬 파일은 한 사람씩 만진다. 만지기 전에 팀에 알린다.

`GIT_협업규칙.md` 에 더 자세히 적혀 있다.
