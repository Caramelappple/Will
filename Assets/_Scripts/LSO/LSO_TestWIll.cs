using _Scripts.LSO.Animal.Data;
using UnityEngine;

public class LSO_TestWIll : LSO_Will
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
