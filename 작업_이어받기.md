# 작업 이어받기

이시온(LSO) 브랜치. 코드는 다 이어져 있고 **씬 배선과 미술 몇 개**가 남았다.

---

## 1. 스테이지 한 바퀴 — 코드는 이어져 있다

```
KTH_GameEndManager      적 전멸 → LSO_StageFlow.ClearStage()
LSO_StageFlow           → flipDirector.Play()            판이 뒤집힌다
LDY_BoardFlipDirector   회전 끝 → Finished
LSO_StageFlow           그 신호를 듣고 → box.Begin()      보상 시작
LSO_RewardBox           정리 끝 → OnFinished
LSO_StageIntroDirector  → 기물 배치 → PlayReverse()      판이 돌아온다
```

**인스펙터로 걸 것은 없다.** 전부 코드가 서로를 찾아 구독한다.

### 씬에 있어야 하는 것

| 없으면 | 증상 |
|---|---|
| `LSO_StageProgression` | "시작할 스테이지가 없습니다" |
| `LSO_StageFlow` | 클리어해도 아무 일 없음 |
| `LDY_BoardFlipDirector` | 회전을 건너뛰고 보상으로 |
| `LSO_RewardBox` | 보상을 건너뛰고 다음 스테이지로 |
| `LSO_StageIntroDirector` | 보상 뒤 판이 안 돌아옴 |

`LSO_StageProgression` 의 **Chapters** 에 `_SO/LSO/Stage/LSO_Chapter.asset` 이 들어 있어야 한다.
그 에셋에는 테스트 스테이지 4개를 넣어뒀다(TEst1~3 + BossStage_BullKing).
**목록의 마지막 칸이 보스로 잡힌다.**

### 확인 방법

`LSO_StageFlow` 의 **Log Steps** 를 켜면 이렇게 찍힌다.

```
클리어 — Est1
보드 회전
보상 시작
```

뒷 절반은 `LSO_StageIntroDirector` 의 같은 스위치로 본다.
전투 없이 돌려보려면 `KTH_TestClearButton`.

---

## 2. 촛불 유언 시스템 — 배선이 남았다

### 조작

```
숫자키 1~N     불꽃 색이 바뀐다 (해금 순서, 마지막 번호가 '유언 없음')
카드 클릭      확정한다 (마우스만 올린 것은 확정이 아니다)
양초 클릭      그 카드로 가서 불을 댄다
카드를 놓는다   그 유언으로 소환된다
```

카드 한 장은 **전투당 한 번**만 받는다. 다음 스테이지가 세워지면 풀린다.

### 양초 오브젝트

```
LSO_WillCandle           숫자키 · 목록
LSO_WillCandleView       색 · 밝기      (Flame 에 Light + LSO_CandleFlicker)
LSO_WillCandleMotion     카드로 다가가기 (없어도 됨)
LSO_WillPainter          붙이기 (클릭 대상)
LSO_ButtonClickHandler   Painter 가 요구하므로 자동
Collider                 없으면 클릭이 안 온다
```

씬에 **EventSystem**, 카메라에 **Physics Raycaster** 가 있어야 3D 클릭이 온다.

### 손패 카드 프리팹

```
LSO_CardWill             카드가 들고 있는 유언
LSO_WillRevealEffect     아이콘 자리(SpriteRenderer)에
```

카드가 풀에서 돌아올 때 `LSO_CardWill.Clear()` 를 불러야 지난 판 유언이 안 남는다.
**이건 아직 안 걸었다** — 태호 쪽 카드 재사용 경로다.

### 남은 미술

- **그을음 스프라이트 1장** — 없으면 "안 고른 것"과 "없음을 고른 것"이 화면에서 같다
- **불꽃 색** — 계약·저주는 흰색 그대로, 계승은 비어 있다. 셋이 같은 흰 불로 보인다
- 디졸브 머티리얼(`LDY/Dissolve` 셰이더)은 **없어도 된다.** 없으면 투명도로 페이드한다

---

## 3. 다시 밟으면 안 되는 함정

### 격자 계산은 판이 앞면일 때만 맞다

`LDY_BoardManager.GridToWorld` 는 `boardOrigin.position` 을 원점으로 쓰는데,
그 `boardOrigin` 이 곧 회전하는 당사자다. 뒤집히면 원점이 축 반대편으로 넘어간다.

뒤집힌 상태에서 좌표가 필요하면 **`LDY_BoardFlipDirector.RunAtHomePose(...)`** 를 거칠 것.
같은 자리에서 두 번 밟았다 — `BoardCenter` 와 다음 스테이지 기물 배치.

### 값은 지금, 연출은 나중에

유언을 붙일 때 **값은 즉시** 넣고 화면만 기다린다.
연출이 끝나기 전에 카드를 놓아도 유언이 빠지면 안 된다.

같은 이유로 `LDY_CardPlacer` 는 유언을 **값이 아니라 `LSO_CardWill` 컴포넌트로** 들고 있다가
놓는 순간에 읽는다. 값으로 붙잡아뒀다가 "붙였는데 기본값으로 소환되는" 일이 있었다.

### 정상 상태를 경고로 내지 말 것

덱 비었음 · 드로우 소진 · 손패 참 은 규칙대로 돌아간 결과다.
`LSO_RejectSignal.Raise(...)` 로 알린다. 콘솔 경고는 **배선이 빠졌을 때만.**

프로퍼티 게터에 로그를 두지 말 것 — 레이아웃에서 매 프레임 읽혀 콘솔이 덮인다.

### 중단 경로

연출을 만들 때마다 **"지금 이게 도는 중에 오브젝트가 꺼지면?"** 을 자문할 것.
이번에 그 종류로 네 번 물렸다(호버 · 트윈 · 코루틴 플래그 · 뚜껑 자세).

---

## 4. 남의 파일 — 병합 전에 알릴 것

### 김태호 (KTH)

```
KTH_HandCard.cs                    ConfirmedCard 추가, Anchor 의존 제거 → Base Scale 필드
KTH_HandCardSelectionController.cs ConfirmedCard 추가
KTH_HandCardLayout.cs              놓을 때 LSO_CardWill 을 넘김
KTH_DoubleClick.cs                 카드 정보창에 붙인 유언 반영
KTH_DeckManager.cs                 정상 상태 경고 → LSO_RejectSignal
KTH_SpawnCard.cs                   중복 경고 제거
KTH_BossPhaseEffectHook.cs         이펙트 정리·부모 잡기
KTH_HandCardScaleSetting.cs        삭제 (어디에도 안 붙어 있었음)
```

### 주다림 (DLJ)

```
DLJ_WillDataSO.cs        displayName · flameColor 추가
DLJ_InfoPanelData.cs     특성 설명 배선 수정, TryFromCard 에 willOverride
DLJ_InfoPanel.cs         Show(card, will) 오버로드
WillData/*.asset (5개)   한글 이름 채움
```

### 이도윤 (LDY) — 인수받음

`CLAUDE.md` 에 담당 이관을 적어뒀다. `LDY_` 접두사는 그대로 두고, 새 파일만 `LSO_` 를 붙인다.
남은 질문은 `LDY_인수_질문목록.md` 에 둘 있다.

---

## 5. 진단 스위치

문제가 생기면 켜서 어디서 끊기는지 본다. 전부 인스펙터 체크박스다.

| 컴포넌트 | 스위치 | 보이는 것 |
|---|---|---|
| `LSO_StageFlow` | Log Steps | 클리어 → 회전 → 보상 |
| `LSO_StageIntroDirector` | Log Steps | 배치 → 되돌리기 |
| `LSO_WillCandle` | Log Steps | 유언 목록, 숫자키 |
| `LSO_WillPainter` | Log Steps | 양초 클릭이 어디까지 갔나 |
| `LDY_CardPlacer` | Log Will | 어떤 유언으로 소환됐나 |
| `LSO_RewardBox` | Log Phases | 상자 단계 |
| `LDY_BoardFlipDirector` | F10 재생 / F11 되돌리기 | 회전만 따로 |

---

## 6. 아직 안 된 것

- **실제 스테이지 SO** — 지금 챕터에 든 넷은 테스트용이다(적 1기씩, 이름도 잘려 있음)
- **갈래 연출** — `On Chapter Changed`(지지직 + 암전 + 지역 이름), `On Boss Stage`(백색 + 보스 이름)가 비어 있다. 보스 이름·이명 데이터도 없다
- **`LSO_RejectSignal` 구독자** — 지금은 아무도 안 듣는다. 카드 흔들기나 소리를 붙이면 코스트 부족·잘못된 칸과 한자리에서 처리된다
- **유언 이름 표시** — 양초가 색만 있고 이름이 없다. `LSO_WillCandle.On Changed` 에 `LSO_WillText.NameOf` 를 거쳐 띄우면 된다
- **`LSO_RewardBox` 763줄** — 로직은 다 나뉘었고 남은 건 인스펙터 설정과 배선이라 그대로 뒀다
