# Git 협업 규칙 (머지 충돌 줄이기)

`base`에 머지할 때마다 충돌이 나던 원인과 대응을 정리한 문서다.
**아래 "1회 설정"은 팀원 각자가 자기 PC에서 한 번씩 실행해야 한다.**

---

## 1회 설정 (각자 한 번씩)

프로젝트 폴더에서 우클릭 → **Git Bash Here** → 아래 한 줄:

```bash
./setup-git.sh
```

끝이다. 여러 번 실행해도 안전하다.

> `.gitattributes` / `.gitignore` / `.editorconfig`는 저장소에 커밋되어 있으므로
> **pull만 하면 자동 적용된다.** 아래 두 가지만 로컬 설정이라 각자 실행이 필요하다.
>
> 실행하지 않아도 **깨지지는 않는다.** 머지 드라이버가 없으면 git이
> 지금까지 하던 텍스트 머지로 그냥 돌아간다. 안 하면 손해일 뿐이다.

### 스크립트가 하는 일 ①: Unity SmartMerge 등록

`.gitattributes`에 씬/프리팹을 `merge=unityyamlmerge`로 지정해 뒀지만,
그 이름에 대응하는 **드라이버 실체는 `.git/config`에 있어서** 각자 등록해야 동작한다.
(`.git/config`는 커밋되지 않는다. 그래서 클론마다 따로 해야 한다.)

스크립트가 `ProjectSettings/ProjectVersion.txt`를 읽어 설치된 유니티에서
`UnityYAMLMerge.exe`를 자동으로 찾는다. 못 찾으면 수동 명령을 안내한다.

등록 확인:

```bash
git config --get-regexp '^merge\.unityyamlmerge'
```

### 스크립트가 하는 일 ②: TMP 폰트 폴백 에셋 추적 제외

`LiberationSans SDF - Fallback.asset`은 플레이 모드에서 새 글자가 나올 때마다
유니티가 글리프 아틀라스를 다시 구워 넣는다. 사람이 고친 게 아닌데
사람마다 다르게 생성되니 만날 때마다 충돌한다.
(브랜치 전체에서 18번 바뀌었고, 커밋한 사람이 4명이었다.)

파일은 저장소에 남겨두되(GUID 참조가 깨지므로), 로컬 변경은 무시하도록 한다.
이미 로컬에서 재생성되어 더러워져 있으면 스크립트가 먼저 되돌린 뒤 설정한다.

되돌리려면 `git update-index --no-skip-worktree "<경로>"`.

---

## 상시 규칙

### 씬은 한 사람씩

`KTH_StageScene.unity` 하나를 3명이 33번 커밋했다.
SmartMerge를 걸어도 같은 오브젝트를 동시에 만지면 깨진다.

- 씬을 만지기 전에 팀에 알린다.
- 큰 UI 덩어리는 프리팹으로 빼서 각자 프리팹을 건드린다.

### 코드 이동/재포맷은 커밋을 분리한다

`KTH_DeckManager.cs`를 `namespace`로 감싸고 전체를 4칸 더 들여쓴 변경이 있었다.
의미상 차이는 몇 줄인데 diff는 810줄이 됐고,
결국 `e831532` 머지에서 **두 버전이 통째로 섞여 파일이 깨진 채 커밋됐다**
(`CanDraw`, `DrawsUsedThisTurn`, `OnDrawLimitChanged`가 중복 정의됨).

- Rider의 "폴더 구조에 맞게 네임스페이스 옮기기" 제안을 수락하지 말 것.
  `.editorconfig`에서 껐지만 수동으로는 여전히 가능하다.
- 포맷만 바꾸는 커밋과 로직을 바꾸는 커밋을 절대 섞지 않는다.
- 파일 전체 재포맷이 꼭 필요하면 팀에 먼저 알리고,
  다들 자기 브랜치를 base에 머지한 직후에 한다.

### 머지 충돌 해결 후에는 반드시 컴파일을 확인한다

`e831532`는 충돌 마커만 지우고 커밋된 것으로 보인다.
유니티 콘솔을 열어 에러 0을 확인한 뒤에 push한다.

### 자동 생성 파일은 커밋하지 않는다

`.idea/`, `.vscode/`, `*.csproj`, `*.sln`은 `.gitignore`에 있다.
이미 추적 중이던 `.idea/`, `.vscode/`는 추적 해제했다.

### base를 자주 받아온다

각자 브랜치에서 작업 중이라도 하루에 한 번은:

```bash
git fetch origin
git merge origin/base
```

오래 묵힐수록 충돌 덩어리가 커진다.
지금 히스토리는 `base`가 각 브랜치를 머지하고 각 브랜치도 `base`를 머지하는
criss-cross 구조라, **한 번 해결한 충돌이 다음 머지에서 또 나타난다.**
자주 맞추는 것 말고 뾰족한 수가 없다.
