Built-in image_gen prompt:
Use case: precise-object-edit. Edit this exact card image by cutting only its four OUTERMOST corners into gentle rounded corners, radius 45 pixels approximately (4% of width). Actual transparent alpha outside the rounded silhouette. Card MUST stay touching all four canvas edges with ZERO added exterior margin: top, bottom, left, right edges unchanged. Only four small corner triangles are cut away. Keep existing black decorative frame and ALL interior pixels, icons, empty parchment, ring, skull, sword, arrow and heart exactly unchanged. Do not redraw, add shadows, add a background, add a border, add text or numbers, or resize the card. Deliver RGBA PNG with real alpha 0 at the four canvas corners and opaque parchment elsewhere. NO checkerboard pattern baked into pixels. Only rounding outer corners.

PNG finishing: Restored original card pixels to prevent interior changes and replaced the four corner regions with an antialiased 45-pixel-radius alpha mask using System.Drawing. No surrounding margin. RGBA PNG, corner alpha 0, interior alpha 255.
